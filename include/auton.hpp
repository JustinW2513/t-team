#pragma once

#include "config.hpp"

void auton() {
    
    /*chassis.setPose(-51.703, -11.825, 90);
	intake.move(127);
	chassis.moveToPoint(-37, -20, 10000, {.maxSpeed=70, .minSpeed=60, .earlyExitRange=5});
	pros::delay(1000);
	chassis.moveToPoint(-19.5, -10, 1000, {.maxSpeed = 70, .minSpeed=60, .earlyExitRange=1});
    chassis.turnToHeading(40, 1000, {.maxSpeed=80, .minSpeed=60});
    intake.move(-127);
    pros::delay(1000);
    intake.move(0);
    chassis.moveToPoint(-21.5, -12, 1000, {.forwards=false, .maxSpeed = 80, .minSpeed=60});
	chassis.turnToHeading(120, 1000, {.maxSpeed=80, .minSpeed=60, .earlyExitRange=10});
    chassis.moveToPoint(-9.0309, -45.94, 500, {.maxSpeed=80});
	//chassis.waitUntilDone();
	

	chassis.moveToPoint(-42, -11.825, 300, {.forwards=false, .maxSpeed=80, .minSpeed=60, .earlyExitRange=5});
	chassis.turnToHeading(50, 700, {.direction = lemlib::AngularDirection::CCW_COUNTERCLOCKWISE, .maxSpeed = 100, .minSpeed=60, .earlyExitRange=3});
	chassis.moveToPoint(-20, -9, 200, {.maxSpeed = 80, .earlyExitRange=4});
	chassis.waitUntilDone();
	intake.move(-127);
	pros::delay(1900);
	intake.move(0);

	chassis.moveToPoint(-21, -21.866, 800, {.forwards=false, .maxSpeed = 80, .minSpeed=60, .earlyExitRange=5});
	chassis.turnToHeading(150, 1000, {.maxSpeed=80, .minSpeed=60, .earlyExitRange=15});
	chassis.moveToPoint(-9.0309, -45.94, 500, {.maxSpeed=80});

	intake.move(300);
	pros::delay(1000);
	intake.move(0);

	chassis.moveToPoint(-21.008, -28.275, 1000, {.forwards=false, .maxSpeed=100, .minSpeed=60, .earlyExitRange=1});
	chassis.turnToHeading(240, 500, {.minSpeed=60, .earlyExitRange=10});
	chassis.moveToPoint(-46, -42, 1000, {.maxSpeed=100, .minSpeed=60, .earlyExitRange=10});
	chassis.turnToHeading(270, 500, {.minSpeed=60, .earlyExitRange=10});

	tongueMech.toggle();
	chassis.moveToPoint(-65, -42, 300, {.maxSpeed=70, .minSpeed=70});
	chassis.waitUntilDone();
	pros::delay(200);

	intake.move(300);
	pros::delay(2000);
	intake.move(0);

	chassis.moveToPoint(-47.925, -38.5, 700, {.forwards=false});
	chassis.waitUntilDone();
	lift.toggle();
	pros::delay(300);
	indexer.toggle();
	pros::delay(200);

	chassis.turnToHeading(92, 700, {.maxSpeed=100});
	tongueMech.toggle();
	pros::delay(100);
	chassis.moveToPoint(-37, -38.9, 430, {.maxSpeed=100});
	chassis.waitUntilDone();
	pros::delay(200);
	intake.move(300);
	pros::delay(2000);
	intake.move(0);
	indexer.toggle();*/
}